//if & nested if construct
if (condition) {}
 else if (another condition) {} 
else {}

//switch case statement
switch (variable) {
     case label: 
    // Statement
    break;

    default:
    // Statement
    break;
}

//?: Conditional operator/ternary operator
expression1 ? expression2 : expression3
